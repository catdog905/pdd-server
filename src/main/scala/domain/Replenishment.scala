package ru.catdog905
package domain

final case class Replenishment(
                        ticketId: String,
                        userName: UserName,
                        reward: Int
                        )
